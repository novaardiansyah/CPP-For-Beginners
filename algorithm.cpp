#include <iostream>
#include <string>
#include <algorithm>
#include <math.h>

using namespace std;

int main()
{
  // ! min() and max() : returns the minimum and maximum values
  // double x = 22, y = 33;
  // cout << "minimum value: " << min(x, y) << endl;
  // cout << "max value: " << max(x, y);

  // ! pow() : returns the power result
  // double x = 22, y = 33;
  // cout << "power result " << x << " and " << y << " is: " << pow(x, y);

  cout << endl;
  return 0;
}